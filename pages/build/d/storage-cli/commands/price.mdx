---
description: Funds the node with the specified amount of tokens.
---

import { Callout } from "nextra-theme-docs";

# `price`

Checks the price to upload the specified number of bytes.

## Parameters

- `-t`: The [token](/overview/supported-tokens) used for payment.
- `-n`: The network to check. Use `testnet`.
- `--provider-url`: RPC URL to use. Required when using testnet.

## Example

```console
irys price 1000000 \
  -t ethereum \
  -n testnet \
  --provider-url https://rpc.sepolia.dev
```

<Callout type="info" emoji="ℹ️">
	RPC URLs change often, use a recent one from https://chainlist.org/
</Callout>
