---
description: Funds the node with the specified amount of tokens.
---

import { Callout } from "nextra-theme-docs";

# `fund`

Fund Irys with the specified amount of tokens.

<Callout type="info" emoji="ℹ️">
Not all calls to `fund` will post immediately to your account, some blockchains are faster than others. 
</Callout>

## Parameters

- `-n`: The network to check, use `testnet`.
- `-t`: The [token](/build/d/features/supported-tokens) to use when funding.
- `-w`: Your private key.
- `--provider-url`: RPC URL to use. 

## Example

```console
irys fund 1000000000000000 \
  -n testnet \
  -t ethereum \
  -w bf20......c9885307 \
  --provider-url https://rpc.sepolia.dev
```

<Callout type="info" emoji="ℹ️">
	RPC URLs change often, use a recent one from https://chainlist.org/
</Callout>
