---
description: Install using npm, optionally sudo.
---

# Installation

Install using npm with the -g global flag. 

```console
sudo npm i -g @irys-network/bundler-sdk
```

Depending on your setup, you may need to use the sudo command.

```console
sudo npm i -g @irys-network/bundler-sdk
```
