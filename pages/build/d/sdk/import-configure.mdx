---
description: Importing and configuring the SDK
---

import { Tab, Tabs, Callout } from "nextra-theme-docs";


# Importing & Configuring

The Irys SDK reduces dependency bloat by providing dedicated packages for each token. Your import statements and connection code will differ depending on the token used for payment.

Select the appropriate code from the options below when starting your project.

<div className="flex justify-center text-center mt-5">
[APTOS](#aptos) | [ALGOrand](#algorand) | [Arbitrum](#arbitrum) | [Avalanche C-Chain](#avalanche-c-chain) | [Berachain](#berachain) | [Binance Coin](#binance-coin) | [Boba](#boba) | [Boba-eth](#boba-eth) | [Chainlink](#chainlink) | [Eclipse](#eclipse) | [Ethereum](#ethereum) | [Base Ethereum](#base-ethereum) | [Linea Ethereum](#linea-ethereum) | [Scroll Ethereum](#scroll-ethereum) | [Fantom](#fantom) | [IoTeX](#iotex) | [Near](#near) | [Polygon](#polygon) | [Solana](#solana) | [USDC (on Ethereum)](#usdc-on-ethereum) | [USDC (on Polygon)](#usdc-on-polygon)
</div>

## Aptos

```ts
import AptosNodeIrys from "@irys-network/node-bundler-aptos";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await AptosNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Algorand

```ts
TODO
```

## Arbitrum

```ts
import ArbitrumNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await ArbitrumNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Avalanche C-Chain

```ts
TODO
```

## Berachain

```ts
import BeraNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await BeraNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Binance Coin

```ts
TODO
```

## Boba

```ts
TODO
```

## Boba-eth

```ts
TODO
```

## Chainlink

```ts
import ChainlinkNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await ChainlinkNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Eclipse

```ts

```

## Ethereum

```ts
import EthereumNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await EthereumNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Base Ethereum

```ts
import BaseEthNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await BaseEthNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Linea Ethereum

```ts
import LineaEthNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await LineaEthNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Scroll Ethereum

```ts
import ScrollEthNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await ScrollEthNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Fantom

```ts
TODO
```

## IoTeX

```ts
import IotexNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await IotexNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Near

```ts
TODO
```

## Polygon

```ts
import MaticNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await MaticNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## Solana

```ts
import SolanaNodeIrys from "@irys-network/node-bundler-solana";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await SolanaNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## USDC (on Ethereum)

```ts
import USDCEthNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await USDCEthNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```

## USDC (on Polygon)

```ts
import USDCPolygonNodeIrys from "@irys-network/node-bundler-ethereum";

const getIrys = async () => {
  // RPC URLs change often, use a recent one from https://chainlist.org/
  const providerUrl = "";

  const irysClient  = await USDCPolygonNodeIrys()
    .withWallet(process.env.PRIVATE_KEY)
    .testnet()
    .withRpc(providerUrl);

  console.log(`Connected to Irys from ${irysClient.address}`);
  return irysClient;
};
```



